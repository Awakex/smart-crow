import { View } from "react-native";
import CustomText from "../../custom-text";

export const NotificationTaskResolve = () => {
    return (
        <View>
            <CustomText text={"text noti"} color={"white"} />
        </View>
    );
};
